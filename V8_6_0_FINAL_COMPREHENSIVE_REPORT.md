# Newtrition V8.6.0 — التقرير النهائي الشامل

**التاريخ:** 2026-08-17
**الطريقة:** مراجعة شاملة للكود + بناء قاعدة بيانات حقيقية من الصفر + تشغيل متصفح Chromium فعلي (Playwright) على السيناريوهات الكاملة، وليس فقط استدعاءات API.

هذه النسخة هي **تجميع كل التطوير والإصلاحات** من V8.5.1 حتى الآن، بعد مراجعة نهائية دقيقة اكتشفت 4 أخطاء إضافية حقيقية لم تظهر إلا بتشغيل متصفح فعلي — رغم أن كل الاختبارات السابقة (API، قاعدة البيانات، الكود الثابت) كانت ناجحة 100%.

---

## 1. لماذا احتجنا لمتصفح فعلي رغم نجاح كل الاختبارات السابقة

كل الإصلاحات من V8.5.2 وحتى V8.5.6 اتحقق منها عبر استدعاءات API مباشرة (curl / fetch) وسكريبتات E2E. هذه الطريقة تثبت أن **السيرفر** يعمل بشكل صحيح، لكنها **لا تكتشف أخطاء الواجهة نفسها** — زرار مربوط بدالة غلط، أو JavaScript بيتوقع شكل بيانات مختلف عن اللي السيرفر بيرجعه فعلاً. هذه الفئة من الأخطاء لا تظهر إلا بمحاكاة نقرة مستخدم حقيقية في متصفح حقيقي.

بنيت 3 سكريبتات Playwright كاملة (موجودة في `tests/`) تحاكي:
1. **`browser_full_workflow.js`** — تسجيل عميلة، تحميل بياناتها، بناء خطة يوم كامل، بدائل، حفظ، بحث، مساعد AI، قيود، مراجعة (31 اختبار).
2. **`browser_plan_lifecycle.js`** — دورة حياة الخطة الكاملة: DRAFT → IN_REVIEW → APPROVED → RELEASED (5 اختبارات).
3. **`browser_client_journey.js`** — من تسجيل العميلة حتى دخولها هي شخصياً على بوابتها ومشاهدة خطتها (8 اختبارات).

---

## 2. الأخطاء الأربعة الجديدة المكتشفة والمُصلَّحة هذه الجلسة

### أ) `api()` كانت بتقرأ حقل غلط من رد الخطأ
كل نداء فاشل في التطبيق كله كان بيرجع رسالة "request_failed" بدل السبب الحقيقي، لأن الكود كان بيدور على `.message` بينما السيرفر بيرجع `.error`. **الأثر الأخطر:** بوابة الجودة السريرية كانت بترفض إرسال أي خطة فيها صنف بيانات غير موثقة، لكن الرسالة اللي بتظهر للطبيب كانت غامضة تماماً من غير أي تفسير.

**الإصلاح:** `api()` دلوقتي بتقرأ `.error` الحقيقي، ومعاها قاموس ترجمة عربي (`ERROR_AR`) لأكثر من 15 كود خطأ معروف.

### ب) بوابة الجودة كانت بترفض من غير تفسير — دلوقتي بتوضح بالظبط
أضفت `explainBlockers()` بتحوّل رد السيرفر التقني (`{code: "unresolved_items", items: [...]}`) لرسالة عربية واضحة تقول أنهي صنف في أنهي وجبة والمشكلة فيه إيه بالظبط، مع اقتراح الحل. وكمان خليت قائمة "اختر صنف" بترتب الأصناف الموثقة كويس (verified/calculated) أول القائمة، وتحذّر بعلامة ⚠ على أي صنف هيمنع اعتماد الخطة.

**التحقق:** بنيت خطة يوم كامل بمتصفح فعلي، أرسلتها للمراجعة، اعتمدتها، أصدرتها — الدورة كاملة نجحت من غير أي تدخل يدوي.

### ج) زرار الدخول في بوابة العميلة كان معطّل بالكامل
```js
const login = async()=>{...}     // متغير JS جديد
...
login.onclick = login;           // ده بيربط الحدث بالـ function نفسها، مش بالزرار!
```
العنصر `<button id="login">` بيعمل متغير عالمي ضمني `window.login`، لكن التصريح الصريح `const login=...` بياخد الأولوية. النتيجة: **زرار "دخول" في بوابة العميلة مكنش بيعمل حاجة خالص من أول ما اتعمل الملف.** هذا الخطأ ما كانش ممكن يظهر إلا بمحاكاة نقرة فعلية في متصفح — كل اختبارات الـ API كانت بتنجح لأنها بتنادي `/api/client-auth/login` مباشرة من غير المرور بالزرار.

**الإصلاح:** غيّرت اسم الدالة لـ `doLogin`، وربطتها بـ `addEventListener` بدل `onclick` المباشر (أكثر أمانًا ضد نفس المشكلة مستقبلاً)، وضفت أداة تحليل ثابت جديدة (`test_frontend_audit.js`) تكتشف أي تعارض مستقبلي بين `id` وأي متغير JS تلقائيًا — واتأكدت إنها بتمسك هذا الخطأ بالظبط في النسخة القديمة.

### د) خطة العميلة المُصدرة كانت تفشل تظهر في بوابتها — بصمت تام
السيرفر بيرجّع `{plan, days, items}` كـ 3 حقول منفصلة على نفس المستوى. لكن كود العرض كان بيتوقع `day.items` (يعني كل يوم معاه الأصناف بتاعته جوّاه)، وهي مش موجودة بهذا الشكل. النتيجة: أي محاولة عرض كانت بترمي خطأ JavaScript يتم "ابتلاعه" بصمت في catch block، فتظهر رسالة "لا توجد خطة منشورة حاليًا" **حتى لو الخطة موجودة وصادرة فعلاً**.

هذا خطأ خطير لأنه بيبدو كحالة طبيعية (مفيش خطة) بدل ما يبان كخطأ برمجي — العميلة كانت هتفتكر إن دكتورها ما بعتلهاش خطة أصلاً.

**الإصلاح:** أعدت بناء `loadPlan()` لتجميع الأصناف حسب `plan_day_id` بالشكل الصحيح. **التحقق:** فتحت بوابة العميلة بمتصفح فعلي بعد تسجيل دخول حقيقي، وشفت الخطة الكاملة بكل الوجبات الخمسة بأسمائها العربية والسعرات.

---

## 3. خاصية كانت ناقصة بالكامل — أضفتها

**مفيش أي زرار في الواجهة لعمل حساب دخول للعميلة في بوابتها.** الـ endpoint موجود في السيرفر (`POST /api/clients/:id/account`) لكن مفيش طريقة تستخدمه من الواجهة أبداً — يعني بوابة العميلة كلها (رغم إنها شغالة تقنياً) كانت **غير قابلة للوصول عمليًا** لأي عميلة حقيقية.

أضفت كارت كامل في "مساحة العميلة":
- إدخال بريد العميلة
- زرار "إنشاء حساب ومنح كلمة مرور مؤقتة"
- عرض كلمة المرور المؤقتة مرة واحدة بوضوح (لأنها لا تُخزَّن كنص واضح ولا تظهر تاني)
- عرض حالة الحساب (موجود / لسه ما غيّرش كلمة المرور)

**التحقق الكامل:** بمتصفح فعلي — الطبيب سجّل عميلة، بنى خطة، أصدرها، عمل لها حساب، **والعميلة بنفسها فتحت `/client`، سجّلت دخول بكلمة المرور المؤقتة، وشافت خطتها الكاملة**.

---

## 4. أداة جديدة: تحليل ثابت للواجهة (`test_frontend_audit.js`)

بنيت أداة تفحص `index.html` و `client.html` تلقائيًا بحثًا عن 4 فئات من الأخطاء التي ثبت أنها حصلت فعليًا في تاريخ هذا المشروع:

1. دوال مكررة بنفس الاسم (المشكلة اللي كسرت `approvePlan` و `openNewClient` قديمًا)
2. أزرار `onclick` بتنادي دوال غير موجودة
3. كود JS بيستخدم `id` غير موجود في الصفحة (مشكلة `cClient` القديمة)
4. تعارض بين `id` ومتغير JS بنفس الاسم (مشكلة `login` المكتشفة هذه الجلسة)

**أثبتّ أن الأداة فعليًا بتمسك الأخطاء** بتشغيلها على النسخ القديمة المعروف أنها معطوبة، وأكدت أنها بتطلع نظيفة 100% على النسخة الحالية.

---

## 5. النتيجة الإجمالية — كل الاختبارات

| نوع الاختبار | العدد | النتيجة |
|---|---|---|
| اختبارات API (E2E) | 79 | ✅ كلها ناجحة |
| اختبار السلامة السريرية (بوابة الجودة) | 8 | ✅ كلها ناجحة |
| التحليل الثابت للواجهة | — | ✅ نظيف تمامًا |
| متصفح: السيناريو الكامل (تسجيل → خطة → بحث → AI) | 31 | ✅ كلها ناجحة |
| متصفح: دورة حياة الخطة الكاملة | 5 | ✅ كلها ناجحة |
| متصفح: رحلة العميلة الكاملة (تسجيل دخول ومشاهدة خطتها) | 8 | ✅ كلها ناجحة |
| **الإجمالي** | **131** | **131 نجاح / 0 فشل** |
| أخطاء 500 في سجل السيرفر خلال كل الاختبارات | — | **0** |
| بناء قاعدة بيانات نظيفة (23 migration) | — | ✅ ثابت (idempotent) |

---

## 6. ما الذي لم يُختبر بعد (بصراحة تامة)

- **النشر الفعلي (Production deployment):** كل الاختبارات تمت على بيئة محلية (PostgreSQL 16 محلي، Node مباشر). لم يُختبر النشر على Railway/Docker أو خلف أي reverse proxy فعلي.
- **الحمل المتزامن (concurrency):** لم يُختبر سيناريو دخول عدة أطباء/عميلات في نفس الوقت.
- **جودة التوصيات الغذائية سريريًا:** الاختبارات تتأكد أن النظام لا يتعطل ويحترم القيود (الحساسية، الأدوار الغذائية)، لكنها لا تقيّم الجودة السريرية للتوصيات نفسها — هذا يحتاج مراجعة أخصائي تغذية.
- **فجوة البيانات المعروفة:** 0 من 1966 صنف له بيانات حساسية موثقة، والحصص الغذائية فارغة. هذه أولوية منفصلة موثقة من الجلسات السابقة، ولم تتغير في هذه النسخة.

---

## 7. الخلاصة

هذه هي أول نسخة تم التحقق منها **بمتصفح فعلي من أول خطوة لآخر خطوة** — من تسجيل عميلة جديدة، مرورًا ببناء خطة كاملة واعتمادها وإصدارها، وحتى دخول العميلة نفسها على بوابتها ومشاهدة خطتها. كل مسار رئيسي في المنتج تم تشغيله فعليًا وليس افتراضه من قراءة الكود.
